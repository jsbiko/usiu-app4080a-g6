Flask==2.3.3
requests==2.31.0
Pillow>=10.1.0
Werkzeug==2.3.7
gunicorn==21.2.0
numpy==1.24.3

dlib==19.22.1
face-recognition==1.3.0
opencv-python==4.8.0.76

# Face recognition libraries (optional - app works in demo mode if these fail)
# These may take 10-15 minutes to build on some platforms
# If installation fails, the app will automatically use demo mode
# System dependencies (cmake, build-essential) should be pre-installed on Render